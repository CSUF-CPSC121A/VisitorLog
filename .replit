compile = "clang++ -std=c++17 main.cc -o main"
run = "./main"

hidden = ["main", "**/*.o", "**/*.d", ".ccls-cache", "Makefile", ".clang-format"]

[languages.cpp]
pattern = "**/*.{cc,h}"
[languages.cpp.languageServer]
start = "clangd"
